{
  "name": "provenflow",
  "version": "0.1.0",
  "private": true,
  "description": "ProvenFlow: design, verify (nuXmv) and run state machines for LLM agents: model checking with counterexample replay, generated runtimes with monitors, live views, trace conformance and probabilistic analysis.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/language",
    "packages/extract",
    "packages/server",
    "packages/app"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "langium:generate": "npm run langium:generate -w @provenflow/language",
    "build": "npm run build -w @provenflow/language && npm run build -w @provenflow/extract && npm run build -w @provenflow/server && npm run build -w @provenflow/app",
    "dev": "npm run build -w @provenflow/language && concurrently -n server,app -c blue,magenta \"npm run dev -w @provenflow/server\" \"npm start -w @provenflow/app\"",
    "start": "npm run start -w @provenflow/server",
    "test": "npm test -w @provenflow/language && npm test -w @provenflow/extract && npm test -w @provenflow/server && npm test -w @provenflow/app -- --watch=false",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check:code": "node packages/server/out/cli.js extract . --fail-on warning"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "~6.0.3"
  }
}
